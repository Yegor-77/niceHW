{
  "name": "project-blog",
  "version": "1.0.0",
  "scripts": {
    "start": "parcel src/index.html --port 1256",
    "server": "json-server --watch src/db.json --port 3000",
    "dev": "concurrently \"npm run start\" \"npm run server\""
  },
  "dependencies": {
    "handlebars": "^4.7.9"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "json-server": "^1.0.0-beta.3",
    "parcel": "^2.12.0"
  }
}
